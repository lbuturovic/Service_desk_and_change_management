/*import axios from 'axios';
import authHeader from './auth-header';
const API_URL = 'http://localhost:8080/api/auth/test';
class UserService {
  getPublicContent() {
    return axios.get(API_URL + 'all');
  }
  getUserBoard() {
    return axios.get(API_URL + 'client', { headers: authHeader() });
  }
  getModeratorBoard() {
    return axios.get(API_URL + 'developer', { headers: authHeader() });
  }
  getAdminBoard() {
    return axios.get(API_URL + 'admin', { headers: authHeader() });
  }
  getServiceBoard() {
    return axios.get(API_URL + 'service', { headers: authHeader() });
  }
  getDeveloperBoard() {
    return axios.get(API_URL + 'developer', { headers: authHeader() });
  }
  getApproverBoard() {
    return axios.get(API_URL + 'approver', { headers: authHeader() });
  }
}
export default new UserService();*/